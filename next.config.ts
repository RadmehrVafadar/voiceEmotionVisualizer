import type { NextConfig } from "next";

const nextConfig = {
  output: 'export',
  assetPrefix: '/voiceEmotionVisualizer',
  // basePath: '/voiceEmotionVisualizer', // Uncomment if you use basePath
};

export default nextConfig;